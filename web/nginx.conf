server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: /matches/12 is a client-side route, not a file on disk.
    # Without this, refreshing any deep link returns nginx's 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}